function drv_foo()

% Following code segment, 
% Type inference should have 2 iterations.
	x=0;
	y=x+1;
	x=x+0.5;


% Following code segment, 
% Type inference should have 2 iterations.
% During 2nd iteration, A=1:n -> A(1,:)=1:n 
% 
	n=floor(3^1);
	A=1:n;
	B=ones(n,1);
	Z=A*B;


	
	
end
